# 🎴 Карточная Игра - Загрузочный Экран

## 📋 Описание

Загрузочный экран с анимацией вращающихся карточек, плавным переходом цветов и автоматическим переходом на главный экран после загрузки.

## 🎨 Кастомизация

### 1. **Изменение цветов фона**

Файл: `lib/splash_screen.dart` (строка 23-28)

```dart
/// Цвета фона для переключения
static const List<Color> cardColors = [
  Color(0xFFFF9500), // Оранжевый
  Color(0xFF4CAF50), // Зелёный
  Color(0xFF9C27B0), // Фиолетовый
];
```

**Как менять цвета:**
- Каждый `Color(0xFFRRGGBB)` - это HEX-код цвета в формате ARGB
- `0xFF` - это полная прозрачность (альфа-канал)
- Остальные 6 символов - RGB код цвета

**Примеры популярных цветов:**
- 🔴 Красный: `Color(0xFFE53935)` 
- 🟠 Оранжевый: `Color(0xFFFF9500)`
- 🟡 Жёлтый: `Color(0xFFFFD600)`
- 🟢 Зелёный: `Color(0xFF4CAF50)`
- 🔵 Синий: `Color(0xFF2196F3)`
- 🟣 Фиолетовый: `Color(0xFF9C27B0)`
- ⚫ Чёрный: `Color(0xFF000000)`
- ⚪ Белый: `Color(0xFFFFFFFF)`

### 2. **Изменение скорости анимации**

Файл: `lib/splash_screen.dart` (строка 30)

```dart
/// Длительность одной итерации анимации в миллисекундах (макс 3000 мс = 3 сек)
static const int cycleDuration = 3000;
```

**Как менять скорость:**
- Значение в миллисекундах (1000 мс = 1 секунда)
- **Текущее значение:** 3000 мс = 3 сек (одна полная итерация с 3 карточками)
- Уменьшить значение = ускорить анимацию
- Увеличить значение = замедлить анимацию

**Примеры:**
- Быстро (2 сек): `static const int cycleDuration = 2000;`
- Нормально (3 сек): `static const int cycleDuration = 3000;`
- Медленно (4 сек): `static const int cycleDuration = 4000;`
- Очень медленно (5 сек): `static const int cycleDuration = 5000;`

### **Количество карточек**

Файл: `lib/splash_screen.dart` (строка 33)

```dart
/// Количество карточек
static const int cardCount = 3;
```

Измени число `3` на любое другое значение (например, 2, 4, 5 и т.д.)

### **Тексты загрузки**

Файл: `lib/splash_screen.dart` (строка 38-44)

```dart
/// Тексты загрузки - меняй на свои!
static const List<String> loadingTexts = [
  'Заставляем сканер работать',
  'Распаковываем колоду карт',
  'Выдаём проблемы лидерам',
  'Готовим поле боя',
  'Калибруем кубики удачи',
];
```

**Как менять текст:**
- Просто замени текст в кавычках на свой
- Можешь добавить больше строк в список
- Тексты будут появляться по очереди, переходя от одного к другому с плавной анимацией

**Примеры новых текстов:**
```dart
static const List<String> loadingTexts = [
  'Запускаем игровой движок',
  'Загружаем карты и эффекты',
  'Инициализируем игровую логику',
  'Настраиваем AI противников',
];
```

### **4. Размеры карточек**

Файл: `lib/splash_screen.dart` (строка 239-241 в методе `build` класса `AnimatedCard`)

```dart
Container(
  width: 40,   // Ширина карточки в пикселях
  height: 60,  // Высота карточки в пикселях
  ...
)
```

**Как менять размер:**
- Увеличь `width` и `height` для больших карточек
- Уменьши для меньших карточек
- Пропорция 40x60 (примерно как игральная карта) - оптимальная

**Примеры:**
- Маленькие: `width: 25, height: 45`
- Средние: `width: 40, height: 60` (текущее)
- Большие: `width: 60, height: 90`

### **5. Позиция текста внизу**

Файл: `lib/splash_screen.dart` (строка 160 в методе `build` класса `_SplashScreenState`)

```dart
Positioned(
  bottom: 50,  // Расстояние от низа экрана (в пикселях)
  left: 0,
  right: 0,
  ...
)
```

Измени число `50` на другое значение:
- `bottom: 30` - ближе к низу
- `bottom: 100` - дальше от низа
- `bottom: 70` - между ними

```dart
// Имитируем загрузку (продолжительность зависит от реальной загрузки данных)
Future.delayed(const Duration(seconds: 5), () {
  if (mounted) {
    _completeLoading();
  }
});
```

Измени `5` на нужное количество секунд отображения экрана перед переходом на главную страницу.

## 🎬 Что происходит в анимации

1. **Начало:** Три карточки начинают переворачиваться одна за другой
2. **Переворот:** Каждая карточка плавно вращается (3D эффект)
3. **Цвет:** Фон плавно переходит между оранжевым → зелёным → фиолетовым
4. **Цикл:** После переворота последней карточки цикл повторяется с новым цветом
5. **Завершение:** После 5 секунд экран переходит на главную страницу приложения

## 📝 Структура файлов

```
lib/
├── main.dart              # Главный файл приложения
├── splash_screen.dart     # Загрузочный экран (главное место для кастомизации)
└── ...
```

## 🚀 Быстрый старт изменений

Чтобы быстро изменить внешний вид:

1. **Для новых цветов:** Отредактируй `cardColors` в `splash_screen.dart` строка 23
2. **Для скорости:** Измени `cycleDuration` на строке 30
3. **Сохрани файл и используй Hot Reload** (нажми `R` в терминале или сохрани файл в VS Code)

## 🔧 Подробное объяснение компонентов

### `SplashScreen` (главный виджет)
- Управляет анимацией всех карточек
- Контролирует переключение цветов
- Отслеживает время загрузки

### `AnimatedCard` (отдельная карточка)
- Вращает одну карточку
- Имеет задержку для эффекта "друг за другом"
- Сбрасывается при новом цикле

## 💡 Советы

- Убедись, что цвета хорошо контрастируют для видимости белых карточек
- Если анимация слишком быстра, увеличь `cycleDuration`
- Если хочешь больше карточек - измени `cardCount` и добавь больше цветов в `cardColors`
