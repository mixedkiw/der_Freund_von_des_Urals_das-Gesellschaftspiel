# 🎴 Загрузочный Экран - Быстрая Шпаргалка

## 🎨 Где менять что:

### Цвета фона (строка 23-28)
```dart
static const List<Color> cardColors = [
  Color(0xFFFF9500), // Оранжевый - МЕНЯЙ СЮДА!
  Color(0xFF4CAF50), // Зелёный
  Color(0xFF9C27B0), // Фиолетовый
];
```

**Популярные цвета:**
- `Color(0xFFFF9500)` - Оранжевый
- `Color(0xFF4CAF50)` - Зелёный
- `Color(0xFF9C27B0)` - Фиолетовый
- `Color(0xFF2196F3)` - Синий
- `Color(0xFFE53935)` - Красный
- `Color(0xFFFFD600)` - Жёлтый

---

### Скорость анимации (строка 30)
```dart
static const int cycleDuration = 3000; // 3000 = 3 сек
```

Измени число:
- `2000` = 2 сек (быстро)
- `3000` = 3 сек (нормально)
- `4000` = 4 сек (медленно)
- `5000` = 5 сек (очень медленно)

---

### Тексты загрузки (строка 38-44)
```dart
static const List<String> loadingTexts = [
  'Заставляем сканер работать',  // ← Меняй это!
  'Распаковываем колоду карт',   // ← И это!
  'Выдаём проблемы лидерам',     // ← И это!
  'Готовим поле боя',
  'Калибруем кубики удачи',
];
```

Просто замени текст в кавычках на свой. Тексты будут циклиться в порядке списка.

---

### Размеры карточек (строка 239-241)
```dart
Container(
  width: 40,   // Ширина - МЕНЯЙ СЮДА!
  height: 60,  // Высота - И СЮДА!
```

Примеры:
- `width: 25, height: 45` - маленькие карточки
- `width: 40, height: 60` - средние (текущее)
- `width: 60, height: 90` - большие карточки

---

### Позиция текста внизу (строка 160)
```dart
Positioned(
  bottom: 50,  // Расстояние от низа - МЕНЯЙ СЮДА!
```

- `bottom: 30` - ближе к низу
- `bottom: 50` - нормально (текущее)
- `bottom: 100` - дальше от низа

---

### Время показа экрана (строка 68-72)
```dart
Future.delayed(const Duration(seconds: 5), () {
  //                                    ↑ МЕНЯЙ СЮДА!
  // 5 = 5 секунд, 3 = 3 секунды и т.д.
```

---

## 🚀 Как применить изменения:

1. Отредактируй нужное значение в `lib/splash_screen.dart`
2. Сохрани файл
3. В терминале где запущено приложение нажми `R` для Hot Reload
4. Готово! Изменения применены

---

## 📊 Что происходит на экране:

```
Оранжевый фон     Зелёный фон      Фиолетовый фон
┌──────┬──────┬──────┐  (переход)  ┌──────┬──────┬──────┐
│ 🔄  │      │      │ ════════► │      │ 🔄  │      │
└──────┴──────┴──────┘            └──────┴──────┴──────┘
                                        (ждём)
  Карточка 1     Карточка 2    Карточка 3    Смена цвета

"Заставляем сканер работать"
     ↓ (переход с фейдом) ↓
"Распаковываем колоду карт"
```

---

## 💡 Советы:

✅ Сохранение файла автоматически запускает Hot Reload
✅ Если Hot Reload не помогает, нажми `R` в терминале
✅ Если нужен полный перезапуск, нажми `Ctrl+C` и снова `flutter run -d linux`
✅ Убедись что цвета контрастные (белые карточки должны быть видны)
